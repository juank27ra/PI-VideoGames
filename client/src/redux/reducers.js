const initialState = {

}

export default function rootReducers(state = initialState, action) {
    switch(action.type) {
        // case    :
        //     return {

        //     }

        default:
            return state;
    }

};